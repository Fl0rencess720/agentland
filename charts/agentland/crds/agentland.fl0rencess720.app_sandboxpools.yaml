---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: sandboxpools.agentland.fl0rencess720.app
spec:
  group: agentland.fl0rencess720.app
  names:
    kind: SandboxPool
    listKind: SandboxPoolList
    plural: sandboxpools
    shortNames:
    - sbxp
    singular: sandboxpool
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SandboxPool is the Schema for the sandboxpools API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SandboxPoolSpec defines the desired state of SandboxPool.
            properties:
              profile:
                type: string
              replicas:
                default: 0
                format: int32
                minimum: 0
                type: integer
              sandboxTemplate:
                description: SandboxTemplate defines a generic pod startup template
                  for all sandbox types.
                properties:
                  args:
                    items:
                      type: string
                    type: array
                  command:
                    items:
                      type: string
                    type: array
                  image:
                    type: string
                required:
                - image
                type: object
            required:
            - sandboxTemplate
            type: object
          status:
            description: SandboxPoolStatus defines the observed state of SandboxPool.
            properties:
              readyReplicas:
                format: int32
                type: integer
              replicas:
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
